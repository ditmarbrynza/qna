$('.answers .answer-error-<%= @answer.id %>').html('<%= render 'shared/errors', resource: @answer %>');

<% if !@answer.errors.present? %>
  $('.answers .answer-id-<%= @answer.id %>').remove();
  $('.answers').append('<%= j render @answer %>');
<% end %>