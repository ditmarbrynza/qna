$('.question-error').html('<%= render 'shared/errors', resource: @question %>');

<% if !@question.errors.present? %>
  $('.question-container .question').remove();
  $('.question-container').append('<%=  j render @question %>');
<% end %>
